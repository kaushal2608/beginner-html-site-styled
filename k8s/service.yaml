apiVersion: v1
kind: Service
metadata:
  name: capstone-website-service
spec:
  type: NodePort
  selector:
    app: capstone-website
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30010